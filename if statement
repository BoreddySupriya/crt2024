#include <stdio.h>

int main() {
    int a,b,r;
    scanf("%d %d",&a,&b);
    if(b==0)
    {
        printf("it can't be zero");
        b=1;
    }
    
    r=a/b;
    
    printf("%d",r);
    

    return 0;
}
