#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int n;
    scanf("%d",&n);
    int r1=n%10;
    int r2=n-r1;
    int r3=n-r1+10;
    if(r1>=5)
    {
        printf("%d",r3);
    }
    else
    {
        printf("%d",r2);
    }
    return 0;
}
